extends ../layout

block title
  = `Drink id ${id}`

block content
  a.button.is-info(href=`/drinks`) Back to drinks

  #list
    .column
      h4= `${user}`
      .card
        header.card-header 
          p.card-header-title
            = name